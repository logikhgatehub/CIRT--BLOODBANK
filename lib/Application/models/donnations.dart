class Donation {
  final String user_id, user_name, user_phoneNo;
  final int donation_points;
  final DateTime date;
  final int gallons;
  final int units;
  Donation(this.user_id, this.user_name, this.user_phoneNo, this.donation_points, this.date, this.gallons, this.units);
}